services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_DB: stonks
      POSTGRES_USER: stonks
      POSTGRES_PASSWORD: stonks
    ports:
      - "55432:5432"
    volumes:
      - stonks-postgres-data:/var/lib/postgresql/data

volumes:
  stonks-postgres-data:
